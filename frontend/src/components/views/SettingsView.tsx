export function SettingsView() {
  return (
    <div className="placeholder-view" aria-label="Settings">
      <div className="placeholder-card">
        <h2>Settings</h2>
        <p>
          Application settings are not configurable in this ALPHA. Future
          versions will include account and notification preferences.
        </p>
        <span className="chip chip--neutral">Not available in ALPHA</span>
      </div>
    </div>
  );
}
